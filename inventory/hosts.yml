---
all:
  children:
    calendar_hosts:
      hosts:
        overleaf:
          ansible_host: overleaf              # SSH config alias → 184.73.127.245
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/overleaf.pem
