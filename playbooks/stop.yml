---
- name: Stop service
  hosts: service_hosts
  gather_facts: false

  tasks:
    - name: Stop service
      ansible.builtin.systemd:
        name: "{{ service_name }}"
        state: stopped
      become: true
